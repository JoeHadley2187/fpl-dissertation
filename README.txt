The project uses MongoDB to store lots of data from the FPL API and hosts this on the cloud with
Atlas
I have replaced the password with XXXXXXX to not disclose the password, however
this does the mean the code does not run currently
If the code is required to execute please email me and I will reply with the password
which can replace XXXXXX on line in main.py

Running the code:
First install all requirments
The code can be executed from main with the current configurations - streamlit run main.py
However if you wish to change the configirations you will need to
follow these steps
from root directory -> cd data -> run csv_generator.py -> cd .. -> streamlit run main.py